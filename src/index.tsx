import React from 'react';
import ReactDOM from 'react-dom';
import RumbleApp from './RumbleApp';

ReactDOM.render(
  <React.StrictMode>
    <RumbleApp />
  </React.StrictMode>,
  document.getElementById('root'),
);
